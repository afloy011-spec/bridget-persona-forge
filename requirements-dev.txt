# Что нужно, чтобы ПРОГНАТЬ ПРОВЕРКИ. Отдельным файлом от requirements.txt, и
# это не косметика: прежде CI ставил свой список руками, а requirements.txt не
# содержал даже pytest — то есть `pip install -r requirements.txt && pytest`
# у свежего человека падал с ModuleNotFoundError на первой же команде.
#
# pyyaml нужен по конкретной причине. tests/test_review_regressions.py разбирает
# .github/workflows/ci.yml, чтобы не повторился случай, когда GitHub отверг весь
# файл workflow и прогон закончился за 0 секунд без единой задачи. Тест написан
# через pytest.importorskip("yaml") и в CI МОЛЧА ПРОПУСКАЛСЯ: на машине автора
# pyyaml стоял, в раннере — нет. Сторож, который сторожит только там, где и так
# всё видно, не сторож.
#
# Тяжёлого здесь нет намеренно: insightface и onnxruntime тянут ~300 МБ моделей
# при первом вызове и в проверку репозитория не входят (см. шапку ci.yml).

pytest>=8,<10
pyyaml>=6
ruff>=0.6
