# Всё, что нужно конвейеру. Одним файлом, потому что прежнее деление на
# «рантайм — чистая стандартная библиотека» и «отдельно ворота» было неверным:
# numpy, Pillow и opencv-python требуют ПЯТЬ из восьми ступеней схемы в README
# (select_set, gates, composite_tattoo, lastmile, deliver/contactsheet/
# build_deck), а не только ворота. Человек, поверивший старому файлу, доходил
# до отбора и получал ImportError.
#
# insightface + onnxruntime тоже НЕ опциональны, вопреки прежней формулировке:
# на них держится select_set.py, то есть единственный механизм идентичности во
# всём проекте. Без них скрипт отбора честно скажет «лицо не найдено» на каждом
# кадре и отберёт мусор. Опционально только в одном смысле: ворота identity,
# cohort и age при их отсутствии дают NOT_MEASURED, а не ложный PASS.
#
# Стадии, которым не нужно ничего сверх стандартной библиотеки: prompts.py,
# generate.py, comfy_client.py, build_ui.py, export_docs.py.

numpy>=1.24
Pillow>=10.0
opencv-python>=4.8
insightface>=0.7
onnxruntime>=1.16
